{% extends 'base_settings.html.twig' %} {# Extend the new settings base template #}

{% block title %}Add New Variable{% endblock %}

{% block body_content %} {# Use the block name from base_settings.html.twig #}
<div class="container mx-auto px-4 py-8"> {# Use container classes like admin pages #}
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-semibold">Add New Variable</h1>
        <a href="{{ path('app_variable_index') }}" class="text-blue-600 hover:text-blue-800">&larr; Back to My Variables</a>
    </div>

    <div class="bg-white shadow p-6 rounded-lg">
        {{ include('variable/_form.html.twig', {'button_label': 'Create Variable'}) }}
    </div>
</div>
{% endblock %} {# End body_content block #}
