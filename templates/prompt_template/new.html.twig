{% extends 'base_settings.html.twig' %}

{% block title %}Create New Prompt Template{% endblock %}

{% block body_content %} {# Corrected block name #}
<div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8"> {# Added container #}
    <h1 class="text-2xl font-semibold text-gray-900 mb-4">Create New Prompt Template</h1>

    {# TODO: Add section to display available user variables (needs Tailwind styling) #}
    {# Example:
    <div class="mb-3 p-3 border rounded bg-light">
        <h5>Available Variables</h5>
        {% if user_variables is defined and user_variables is not empty %}
            <ul class="list-inline mb-0">
                {% for variable in user_variables %}
                    <li class="list-inline-item"><code class="user-select-all">{{ '{{' ~ variable.name ~ '}}' }}</code></li>
                {% endfor %}
            </ul>
        {% else %}
            <p class="mb-0 text-muted">No custom variables defined yet.</p>
        {% endif %}
         <small class="d-block mt-2">Use these in your message content.</small>
    </div>
    #}

    <div class="bg-white shadow px-4 py-5 sm:rounded-lg sm:p-6">
        {{ include('prompt_template/_form.html.twig', {'button_label': 'Create Template'}) }}
    </div>

    <div class="mt-4">
        <a href="{{ path('app_prompt_template_index') }}" class="text-sm font-medium text-indigo-600 hover:text-indigo-500">
            &larr; Back to list
        </a>
    </div>
</div>
{% endblock %}
