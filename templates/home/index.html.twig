{% extends 'base.html.twig' %}

{% block title %}AI MultiChat - Compare Multiple AI Models Side by Side{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-b from-gray-900 via-indigo-950 to-gray-900 text-white">
    <!-- Hero Section -->
    <div class="container mx-auto px-4 py-20">
        <div class="text-center max-w-3xl mx-auto">
            <h1 class="text-5xl md:text-6xl font-bold mb-6 bg-clip-text text-transparent bg-gradient-to-r from-blue-400 to-purple-500">AI MultiChat</h1>
            <p class="text-xl md:text-2xl mb-10 text-gray-200">Compare responses from multiple AI models in real-time</p>
            <a href="{{ path('app_login') }}" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-8 rounded-lg inline-flex items-center transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                <svg class="w-5 h-5 mr-2" viewBox="0 0 24 24">
                    <path fill="currentColor" d="M12.545,10.239v3.821h5.445c-0.712,2.315-2.647,3.972-5.445,3.972c-3.332,0-6.033-2.701-6.033-6.032s2.701-6.032,6.033-6.032c1.498,0,2.866,0.549,3.921,1.453l2.814-2.814C17.503,2.988,15.139,2,12.545,2C7.021,2,2.543,6.477,2.543,12s4.478,10,10.002,10c8.396,0,10.249-7.85,9.426-11.748L12.545,10.239z"/>
                </svg>
                Sign in with Google
            </a>
        </div>
    </div>

    <!-- Features Section -->
    <div class="container mx-auto px-4 py-16">
        <div class="grid md:grid-cols-3 gap-8">
            <div class="bg-gray-800 bg-opacity-50 p-8 rounded-xl shadow-lg transition-all duration-300 hover:transform hover:scale-105 hover:bg-opacity-70 border border-gray-700">
                <h3 class="text-xl font-bold mb-4 text-blue-400">Multiple AI Models</h3>
                <p class="text-gray-300">Compare responses from GPT-4, Claude, Llama, and other leading AI models side by side.</p>
            </div>
            <div class="bg-gray-800 bg-opacity-50 p-8 rounded-xl shadow-lg transition-all duration-300 hover:transform hover:scale-105 hover:bg-opacity-70 border border-gray-700">
                <h3 class="text-xl font-bold mb-4 text-blue-400">Real-time Comparison</h3>
                <p class="text-gray-300">See responses stream in real-time as you interact with multiple models simultaneously.</p>
            </div>
            <div class="bg-gray-800 bg-opacity-50 p-8 rounded-xl shadow-lg transition-all duration-300 hover:transform hover:scale-105 hover:bg-opacity-70 border border-gray-700">
                <h3 class="text-xl font-bold mb-4 text-blue-400">Thread Management</h3>
                <p class="text-gray-300">Maintain conversation context and history across all your AI interactions.</p>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="container mx-auto px-4 py-12 text-center text-gray-400 border-t border-gray-800 mt-12">
        <p>&copy; {{ "now"|date("Y") }} AI MultiChat. All rights reserved.</p>
    </footer>
</div>
{% endblock %}
