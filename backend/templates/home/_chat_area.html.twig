{# Chat Area Template #}
<div class="flex flex-col h-full">
    {# Messages List #}
    <div class="flex-1 overflow-y-auto p-4 space-y-4" data-controller="chat">
        <div data-chat-target="messagesList">
            {# Messages will be dynamically inserted here by JavaScript #}
        </div>
    </div>

    {# Message Input #}
    <div class="border-t p-4">
        {% include 'home/_message_input.html.twig' %}
    </div>
</div> 